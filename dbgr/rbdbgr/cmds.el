(require 'load-relative)
(require-relative-list
 '("../common/send") "dbgr-")

(defun dbgr-define-rbdbgr-commands ()
  "(Re)define a bunch of rbdbgr commands have. Nothing here yet."
  )

(provide-me "rbdbgr-")


